local VIS = game:GetService("UserInputService") local plr = game.Players.LocalPlayer local hum UIS. JumpRequest:Connect(function() if not hum then local char = plr.Character if char then hum = char:FindFirstChildOfClass("Humanoid") end end if hum then hum: ChangeState(Enum.HumanoidStateType.Jumping) end end)
